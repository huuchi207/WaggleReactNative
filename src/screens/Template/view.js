import BaseComponent from "../../../helper/base/index";
import { Text, View, Button, Alert } from "react-native";
class Template extends BaseComponent {
  constructor(props) {
    super(props);
  }

  someFunction(){

  }

  render() {
    super.render();
    return (
      <View>

      </View>
    );
  }
}

export default Template;
